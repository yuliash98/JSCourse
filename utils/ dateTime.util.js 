class DateTimeUtil {
    today() {
        return new Date()
    }
    setYear(date, year) {
        return new Date()
    }
    daysDifference(dateLeft, dateRight) {
        return dateRight - dateLeft
    }
 
}

module.exports = new DateTimeUtil()